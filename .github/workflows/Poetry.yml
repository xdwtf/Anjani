name: Update Python Poetry packages

on: workflow_dispatch

jobs:
  update-packages:
    runs-on: ubuntu-latest
    steps:
      - uses: dishapatel010/action-poetry-package-update@V2.0.1
        with:
          base-branch: dishapatel010-patch-1  # Can also be `master`, for example
